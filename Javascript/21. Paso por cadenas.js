// Cadenas y funciones en JS
// Cadenas inmutables en JS (no se pueden modificar directamente)

function cambiarValor(parametro)
{
    parametro = 'Adiós';
}

//Llamamos a la funci[on
let argumento = 'Hola';
console.log(argumento);

cambiarValor(argumento);
console.log(argumento);